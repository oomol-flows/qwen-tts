version: 0.1.0
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
name: "qwen-tts"
displayName: "%display-name%"
description: "%description%"
icon: ./icon.png
ui:
  blocks:
    - qwen-tts
dependencies:
  downloader: 0.1.5
releaseNotes: First stable release
repository: https://github.com/oomol-flows/qwen-tts
